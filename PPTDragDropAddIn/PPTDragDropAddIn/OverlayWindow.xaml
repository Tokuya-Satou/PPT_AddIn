<Window x:Class="PPTDragDropAddIn.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="0" Top="0">
    <Grid x:Name="RootGrid" TouchMove="RootGrid_TouchMove" TouchUp="RootGrid_TouchUp">
        <!-- タッチ吸収レイヤー: Drag_ 図形の領域にタッチ不透明な矩形を配置 -->
        <Canvas x:Name="TouchGuardCanvas" IsHitTestVisible="True" Background="Transparent"/>
        <Canvas x:Name="OverlayCanvas">
            <Image x:Name="DragImage" Visibility="Collapsed" Stretch="Fill" />
        </Canvas>
        <InkCanvas x:Name="DrawingCanvas"
                   Background="Transparent"
                   IsHitTestVisible="False"/>
    </Grid>
</Window>
