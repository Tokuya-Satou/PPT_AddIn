# 開発メモ 2026-04-17

---

## 完了したこと

### リボンUI実装（spec_ribbon_v1.md に基づく）

| 項目 | 内容 | 状態 |
|---|---|---|
| `Properties.Settings` に設定キー追加 | DragDropEnabled / PenPaletteEnabled / LastPenColor / LastMarkerColor | ✅ |
| リボンタブ「PPTドラッグ」追加 | 制御グループ（トグル2つ）・ペン設定グループ（splitButton2つ） | ✅ |
| コールバック実装 | トグルON/OFF・ペン色保存・アイコン動的生成（Bitmap）| ✅ |
| `SlideShowBegin` の ON/OFF 分岐 | トグルOFF → オーバーレイ/パレットを起動しない | ✅ |
| スライドショー開始時の初期ペン色設定 | `GetSavedPenColor()` で DrawingAttributes を事前設定 | ✅ |

### 動作確認済み

- トグル（ドラッグ移動・ペンパレット）の ON/OFF → スライドショー開始時の起動制御 ✅
- ペン色をリボンで変更 → 再起動後にアイコン（色スウォッチ）に反映 ✅
- コンテキストメニュー（右クリック → Drag_ 付与/解除）引き続き動作 ✅

---

## 残っている課題

### 短期（次回セッション）

| 優先 | 内容 | 詳細 |
|---|---|---|
| 高 | ペン色アイコンの即時更新確認 | `InvalidateControl("btnPenMain")` に修正済み（動作未確認） |
| 高 | パレットとリボン設定色の同期 | パレットのボタンがハードコードのため、リボンで選んだ色がスライドショー中に上書きされる。解決策: PenPaletteWindow.xaml.cs のボタンが Settings の色を参照するよう変更 |

### 中期（フェーズ2）

| 内容 | 参照 |
|---|---|
| タッチドラッグのデッドロック解消 | `memo20260404.md` の「次に試すこと」3案 |

### 長期（フェーズ3以降）

| 内容 |
|---|
| パレットのボタンをリボン設定色で動的に更新 |
| ペンプロファイル（Pen 1/2/3）機能 |
| 「このプレゼンでは無効」機能（CustomDocumentProperties） |

---

## 変更ファイル一覧（今回の作業）

| ファイル | 変更内容 |
|---|---|
| `Properties/Settings.settings` | 4設定キー追加 |
| `Properties/Settings.Designer.cs` | プロパティアクセサー追加 |
| `DragDropRibbon.xml` | タブ・グループ・トグル・splitButton追加 |
| `DragDropRibbon.cs` | コールバック・Bitmap色アイコン生成 |
| `ThisAddIn.cs` | SlideShowBegin ON/OFF分岐・GetSavedPenColor追加 |

---

## Git状態

- ブランチ: `dev`
- 未コミット（動作確認中のため保留）
- 次回: 動作確認後に `dev` にコミット → 安定したら `main` にマージ
